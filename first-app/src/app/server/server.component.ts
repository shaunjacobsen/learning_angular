import { Component } from "@angular/core";

@Component({
  selector: 'app-server', // the html selector where the component is to be rendered
  templateUrl: './server.component.html' // the html file containing the component code
})

export class ServerComponent {

}